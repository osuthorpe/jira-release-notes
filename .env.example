# See README.md for step-by-step instructions on getting each of these values.

# OpenAI (required - writes the release notes)
OPENAI_API_KEY=your-openai-api-key
# Optional: which model writes the notes (defaults to gpt-4o)
# OPENAI_MODEL=gpt-4o

# JIRA (required - where the tickets come from)
JIRA_SERVER=https://your-domain.atlassian.net
JIRA_USERNAME=your-email@company.com
JIRA_API_TOKEN=your-jira-api-token
# Advanced: replace the default ticket query (project BPD, label Release_Notes, fix version)
# JIRA_JQL_QUERY=project = "BPD" AND labels = Release_Notes AND fixVersion = "06-12-2026"

# Zendesk (optional - creates a DRAFT article in the Help Center)
ZENDESK_SUBDOMAIN=your-subdomain
ZENDESK_EMAIL=your-email@company.com
ZENDESK_API_TOKEN=your-zendesk-api-token
ZENDESK_SECTION_ID=200825397
# Only needed if your Help Center requires a permission group on articles
# ZENDESK_PERMISSION_GROUP_ID=1234567

# Slack (optional - posts the Zendesk draft link for review; set both or neither)
# SLACK_BOT_TOKEN=xoxb-your-slack-bot-token
# SLACK_CHANNEL=C0123456789
