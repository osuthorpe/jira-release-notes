[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
# pyflakes/pycodestyle, import order, modern syntax, bugbear, simplifications
select = ["E", "F", "W", "I", "UP", "B", "SIM"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
